"use client";
import { useEffect, useMemo, useState } from "react";
import { WorkspaceHeader } from "../workspace-header";
type Patient={id:number;patientCode:string;age:number;sex:string;cancerType:string;stage:string;biomarker:string;status:string;site:string;consentStatus:string};
export default function PatientsPage(){const [items,setItems]=useState<Patient[]>([]);const [query,setQuery]=useState("");useEffect(()=>{void fetch("/api/patients",{cache:"no-store"}).then(async r=>{if(r.ok)setItems((await r.json()).patients)})},[]);const shown=useMemo(()=>items.filter(p=>Object.values(p).join(" ").toLowerCase().includes(query.toLowerCase())),[items,query]);return <main><WorkspaceHeader active="patients"/><div className="subPage"><div className="subIntro actionsIntro"><div><p>PATIENT DIRECTORY</p><h1>Demonstration patients</h1><span>Search consent-aware research records and review cohort-ready clinical attributes.</span></div><div><a className="subPrimary" href="/#cohort-manager">Add patient</a><a className="subSecondary" href="/api/export">Export CSV</a></div></div><section className="panel patientDirectory"><div className="directoryToolbar"><label>Search directory<input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Code, cancer, site, biomarker…"/></label><span>{shown.length} records</span></div><div className="tableWrap"><table><thead><tr><th>Patient</th><th>Age / Sex</th><th>Diagnosis</th><th>Biomarker</th><th>Consent</th><th>Site</th></tr></thead><tbody>{shown.map(p=><tr key={p.id}><td><b>{p.patientCode}</b></td><td>{p.age} / {p.sex}</td><td>{p.cancerType} · Stage {p.stage}</td><td>{p.biomarker}</td><td><span className={`consent ${p.consentStatus.toLowerCase()}`}>{p.consentStatus}</span></td><td>{p.site}</td></tr>)}</tbody></table>{!shown.length&&<div className="subEmpty">No matching demonstration records.</div>}</div></section></div></main>}
